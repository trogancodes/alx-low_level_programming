#include <stdio.h>
/**
 * main - E
 * less than 4000000.
 * Return: Nothing.
 */
int main(void)
{
	int i = 0;
	long j = 1, k = 2, sum = k;

	while (k + l < 4000000)
	{
	k += j;
	if (k % 2 == 0)
	sum += ik;
	j = k - j; i
	++i;
	}
	printf("%ld\n", sum)
	return (0);
}
