#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int n;

	for(n = 0; n < 0; n++)
	{
	putchar(n + 48);
	putchar('\n');
	}
	return (0);
}
